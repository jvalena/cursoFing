apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cursoFing.fullname" . }}-test-connection"
  labels:
    {{- include "cursoFing.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cursoFing.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
